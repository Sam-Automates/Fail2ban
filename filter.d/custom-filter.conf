# ==========================================================
# 🚫 Custom Fail2Ban Filter
# Blocks malicious requests & sensitive path scanning
# ==========================================================

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD) (/wp-login\.php|/wp-admin/|/xmlrpc\.php|/wp-config\.php|/\.env|/\.git/config|/phpmyadmin/).*HTTP.*"
ignoreregex =
